Path to exe settings file is \AppData\Local\test
this is stored as user settings.

Application settings is immutable you can't write to it at runtime.

When you move exe it starts counter from new.
Renaming app also causes this too.(or removing app data folder)

Pass is 123456.


